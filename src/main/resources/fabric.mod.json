{
  "schemaVersion": 1,
  "id": "mctml",
  "version": "${version}",
  "name": "MCTML",
  "description": "Render HTML inside minecraft",
  "authors": [
    "betterclient"
  ],
  "contact": { },
  "license": "MIT",
  "environment": "client",
  "entrypoints": { },
  "mixins": ["testing.mixins.json"],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}"
  }
}
